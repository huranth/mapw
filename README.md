# mapw

Multi-pane terminal workspace — a calm desktop shell for running several terminals side by side.

- `software/` — the desktop app (Electron + React + xterm). Install + docs: [software/README.md](software/README.md).
- `website/` — the public site, deployed on Vercel. Not built yet.
